# MCP (Model Context Protocol) 設置指南

**最後更新**：2025-11-12
**狀態**：✅ Sprint 0.9 Planning

---

## 📖 目錄

- [什麼是 MCP?](#什麼是-mcp)
- [為什麼使用 MCP?](#為什麼使用-mcp)
- [前置要求](#前置要求)
- [Step 1: 獲取 Supabase 訪問令牌](#step-1-獲取-supabase-訪問令牌)
- [Step 2: 配置環境變數](#step-2-配置環境變數)
- [Step 3: 配置 MCP 設置](#step-3-配置-mcp-設置)
- [Step 4: 測試 MCP 連接](#step-4-測試-mcp-連接)
- [常見問題](#常見問題)
- [安全最佳實踐](#安全最佳實踐)
- [相關資源](#相關資源)

---

## 什麼是 MCP?

**Model Context Protocol (MCP)** 是一個開放標準協議，允許 Claude 與外部系統（如 Supabase）進行互動。

在 Flourish 專案中，MCP 讓 Claude Code 能夠直接查詢和操作 Supabase 資料庫，無需手動編寫 SQL 查詢。

### MCP 的核心功能

- **結構化工具訪問**：通過標準化的工具界面與資料庫互動
- **上下文保持**：Claude 可以在開發時獲得實時資料庫狀態
- **自動化操作**：執行數據查詢、遷移管理等任務
- **安全隔離**：使用環境變數和令牌管理保護憑證

---

## 為什麼使用 MCP?

| 優勢         | 說明                                   |
| ------------ | -------------------------------------- |
| **開發效率** | 無需手動切換到 Supabase 控制台查看數據 |
| **減少錯誤** | Claude 可以驗證遷移和查詢的正確性      |
| **實時反饋** | 在 Claude Code 中直接看到數據庫變化    |
| **自動化**   | 自動生成遷移、更新 schema 等           |
| **安全性**   | 使用環境變數管理敏感令牌               |

---

## 前置要求

在開始 MCP 設置之前，確保你已經準備好：

- ✅ Supabase 帳戶（已有 Flourish 專案）
- ✅ Supabase 專案訪問權限
- ✅ Claude Code CLI 已安裝
- ✅ 能夠修改環境變數和配置檔案

### 檢查環境

```bash
# 檢查 Claude Code 是否已安裝
which claude

# 檢查 Supabase CLI（可選，但推薦用於本地開發）
supabase --version
```

---

## Step 1: 獲取 Supabase 訪問令牌

### 1.1 登入 Supabase 控制台

1. 訪問 [Supabase Dashboard](https://app.supabase.com)
2. 使用 Henry Lee 帳戶登入
3. 選擇 **Flourish** 專案

### 1.2 生成個人訪問令牌

1. 在 Supabase 控制台中，點擊右上角的 **帳戶圖示**
2. 選擇 **Settings** → **Access Tokens**
3. 點擊 **Create Token** 按鈕
4. 設置令牌詳情：
   - **Token Name**: `claude-code-mcp` （或類似的名稱）
   - **Expiration**: 選擇合適的過期時間（建議 90 天或自定義）
   - **Scopes**: 確保選擇了必要的權限
     - `read` （讀取資料庫信息）
     - `write` （如果需要執行遷移）

5. 點擊 **Create Token**
6. **立即複製令牌值**（之後不會再顯示）

### 1.3 驗證令牌格式

Supabase 訪問令牌看起來像：

```
sbp_v0_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

注意 `sbp_v0_` 前綴和長字符串。

---

## Step 2: 配置環境變數

### 2.1 建立或更新 .env.local 檔案

在專案根目錄中，建立或編輯 `.env.local` 檔案：

```bash
# 從專案根目錄
cd /Users/henrylee/personal/flourish

# 建立或編輯 .env.local
cat > .env.local << 'EOF'
# Supabase MCP Configuration
SUPABASE_ACCESS_TOKEN=sbp_v0_YOUR_TOKEN_HERE
SUPABASE_PROJECT_REF=fstcioczrehqtcbdzuij

# Claude Code
CLAUDE_PROJECT_CONTEXT=flourish
EOF
```

### 2.2 替換令牌值

將 `sbp_v0_YOUR_TOKEN_HERE` 替換為你在 Step 1.2 中複製的實際令牌值。

### 2.3 驗證 .env.local 不會被提交

檢查 `.gitignore` 已包含 `.env.local`：

```bash
# 檢查 .gitignore
grep "\.env\.local" .gitignore
# 應該輸出：.env.local
```

如果 `.env.local` 未在 `.gitignore` 中，執行：

```bash
echo ".env.local" >> .gitignore
git rm --cached .env.local  # 從 git 中移除（如果之前被提交）
```

---

## Step 3: 配置 MCP 設置

### 3.1 檢查 MCP 配置檔案

MCP 配置已在 `.claude/mcp-settings.json` 中設置。驗證檔案內容：

```bash
cat .claude/mcp-settings.json
```

應該看到類似的內容：

```json
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server"],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}",
        "SUPABASE_PROJECT_REF": "fstcioczrehqtcbdzuij"
      }
    }
  }
}
```

**注意**：

- `${SUPABASE_ACCESS_TOKEN}` 是環境變數佔位符，將從 `.env.local` 讀取
- `SUPABASE_PROJECT_REF` 是固定的專案參考 ID
- 這個檔案 **可以安全地提交** 到 Git，因為不包含實際令牌

### 3.2 驗證 Claude Code 配置

檢查 Claude Code 全局配置是否包含 MCP 設置：

```bash
# 查看 Claude Code 配置
cat ~/.config/claude-code/config.json
```

應該看到 Supabase MCP 伺服器配置。

---

## Step 4: 測試 MCP 連接

### 4.1 在 Claude Code 中測試

在 Claude Code 中執行以下命令，檢查 MCP 連接：

```bash
# 列出所有可用的 MCP 資源
/mcp list-resources
```

如果設置正確，應該能看到 Supabase 資源列表。

### 4.2 測試與 Supabase 的通信

在 Claude Code 對話中，嘗試以下操作：

**詢問數據庫狀態**：

```
@claude 檢查 Supabase 專案的當前連接狀態
```

Claude 應該能通過 MCP 伺服器檢查連接。

**查詢表結構**：

```
@claude 列出 Supabase 中的所有表及其列名
```

Claude 可以使用 MCP 工具檢索表結構信息。

### 4.3 常見測試場景

| 場景       | 預期結果                      | 故障排查       |
| ---------- | ----------------------------- | -------------- |
| 列出表     | 看到 users, transactions 等表 | 檢查令牌有效性 |
| 查看表結構 | 看到列名和類型                | 檢查專案參考   |
| 讀取資料   | 看到表中的行（如果有）        | 檢查 RLS 策略  |
| 執行遷移   | 新表被建立                    | 檢查令牌權限   |

---

## 常見問題

### Q1: 令牌過期了怎麼辦？

**解決方案**：

1. 回到 Supabase 控制台 → Settings → Access Tokens
2. 刪除舊令牌
3. 建立新令牌
4. 更新 `.env.local` 中的令牌值
5. 重新啟動 Claude Code

### Q2: 我得到 "Invalid token" 錯誤

**可能原因**：

- ❌ 令牌複製不完整（缺少字符）
- ❌ 令牌已過期
- ❌ 環境變數未正確設置

**解決方案**：

```bash
# 驗證 .env.local 中的令牌
echo $SUPABASE_ACCESS_TOKEN | wc -c
# 應該輸出約 79 個字符

# 檢查是否正確加載環境變數
env | grep SUPABASE
```

### Q3: MCP 伺服器無法啟動

**可能原因**：

- ❌ `@supabase/mcp-server` 未安裝
- ❌ Node.js 版本不兼容
- ❌ 缺少環境變數

**解決方案**：

```bash
# 安裝 MCP 伺服器（如需要）
npm install -g @supabase/mcp-server

# 檢查 Node.js 版本
node --version
# 應該 ≥ 18.0.0

# 驗證環境變數
source .env.local
echo $SUPABASE_ACCESS_TOKEN
```

### Q4: 看不到我的數據

**可能原因**：

- ❌ Row Level Security (RLS) 策略阻擋了查詢
- ❌ 表中沒有資料
- ❌ 表名輸入錯誤

**解決方案**：

```sql
-- 在 Supabase SQL Editor 中檢查 RLS 狀態
SELECT table_name, row_level_security
FROM information_schema.tables
WHERE table_schema = 'public';
```

---

## 安全最佳實踐

### ✅ 必須做的事

| 項目           | 說明                                  |
| -------------- | ------------------------------------- |
| **環境變數**   | 始終使用 `.env.local` 存儲令牌        |
| **gitignore**  | 確保 `.env.local` 在 `.gitignore` 中  |
| **限制權限**   | 只授予 MCP 令牌必要的權限             |
| **定期輪換**   | 每 90 天更新一次令牌                  |
| **佔位符使用** | 在配置檔案中使用 `${VAR_NAME}` 佔位符 |

### ❌ 不能做的事

| 項目                      | 原因                             |
| ------------------------- | -------------------------------- |
| **硬編碼令牌**            | 令牌會被提交到 Git，造成安全風險 |
| **共享令牌**              | 每個開發者應有自己的令牌         |
| **提交 .env.local**       | 應被 `.gitignore` 保護           |
| **在日誌中打印令牌**      | 防止在控制台中暴露敏感信息       |
| **使用 Service Role Key** | 僅在後端使用（需要額外安全保護） |

### 令牌輪換流程

```bash
# 1. 在 Supabase 控制台建立新令牌
# 2. 更新 .env.local
SUPABASE_ACCESS_TOKEN=sbp_v0_NEW_TOKEN

# 3. 測試連接
source .env.local
# 在 Claude Code 中驗證

# 4. 刪除舊令牌（在 Supabase 控制台）

# 5. 通知團隊成員更新他們的令牌
```

---

## 常見令牌管理場景

### 場景 1：本地開發環境

```bash
# 開發者 A
.env.local
SUPABASE_ACCESS_TOKEN=sbp_v0_HENRY_TOKEN...

# 開發者 B
.env.local
SUPABASE_ACCESS_TOKEN=sbp_v0_OTHER_TOKEN...

# 檔案：.claude/mcp-settings.json （提交到 Git）
${SUPABASE_ACCESS_TOKEN}  ← 讀取各自的 .env.local
```

### 場景 2：更換電腦

```bash
# 1. 克隆專案
git clone ...
cd flourish

# 2. 建立 .env.local
echo "SUPABASE_ACCESS_TOKEN=YOUR_TOKEN" > .env.local

# 3. 測試
source .env.local
# 在 Claude Code 中驗證連接
```

### 場景 3：團隊協作

```bash
# 共享的配置檔案（已提交）
.claude/mcp-settings.json

# 個人的令牌（不提交）
.env.local (gitignored)
```

---

## 高級配置

### 使用 Claude Code 全局配置

如果需要全局配置 MCP（而不是只在 Flourish 專案中），可以編輯：

```bash
~/.config/claude-code/config.json
```

結構與 `.claude/mcp-settings.json` 相同。

### 多個 MCP 伺服器

如果將來需要連接多個 MCP 伺服器（例如 GitHub、Stripe），可以在同一檔案中配置：

```json
{
  "mcpServers": {
    "supabase": { ... },
    "github": { ... },
    "stripe": { ... }
  }
}
```

---

## 疑難排解檢查清單

在聯繫支援之前，確認以下事項：

- [ ] `.env.local` 檔案存在且包含 `SUPABASE_ACCESS_TOKEN`
- [ ] `.env.local` 在 `.gitignore` 中
- [ ] 令牌以 `sbp_v0_` 開頭
- [ ] 令牌未過期（在 Supabase 控制台檢查）
- [ ] `.claude/mcp-settings.json` 存在且格式正確
- [ ] 環境變數已加載：`source .env.local && echo $SUPABASE_ACCESS_TOKEN`
- [ ] Claude Code 已重新啟動（若剛更新環境變數）
- [ ] 沒有拼寫錯誤（變數名、專案參考等）

---

## 相關資源

### 官方文檔

- 📄 [Supabase 官方文檔](https://supabase.com/docs)
- 📄 [MCP 官方文檔](https://modelcontextprotocol.io/)
- 📄 [Claude Code 文檔](https://docs.claude.com/)

### Flourish 項目文檔

- 📄 [部署文檔](../deployment/README.md)
- 📄 [ADR 001 - 架構簡化](../decisions/001-architecture-simplification.md)
- 📄 [Sprint 0.9 規劃](../sprints/sprint-0-foundation/0.9-supabase-migration-plan.md)
- 📄 [Supabase 遷移計劃](../sprints/sprint-0-foundation/0.9-supabase-migration-plan.md)

### 相關指南

- 📄 [Git 工作流程](./git-workflow.md)
- 📄 [資料庫設置](./database-setup.md)
- 📄 [部署指南](./deployment.md)

---

## 支援與反饋

遇到問題或有建議？

- 🐛 **報告問題**：[GitHub Issues](https://github.com/henrylee-work/flourish/issues)
- 💬 **討論**：[GitHub Discussions](https://github.com/henrylee-work/flourish/discussions)
- 📧 **聯繫**：henry@example.com

---

**維護者**：Henry Lee
**最後更新**：2025-11-12
**版本**：1.0 (Sprint 0.9 Planning)
